#!/bin/bash

if [ -d $1 ]
then 
destination_dir=$1
else
echo "Please specify directory"
exit 1
fi

for i in $2 $3 $4
do
echo $i
#if [ $i != [[:digit:]] ]
if [ $i -lt 0 ]
then
echo "pleasy, input digit"
exit 1
fi
done

depth=$2
max_size=$3
iter=$4

line=""
for (( d = 1; d <= $depth; d++))
do
line+="/dir-{0..$iter}--$d"
done

eval "mkdir -p $destination_dir/$line"
eval "touch $destination_dir/$line/file-{0..$iter}"

